import React from 'react'

const CodeSlide = ({children}) => (
  <div id="lala" style={{
      paddingBottom: '-2rem',
      paddingTop: '-2rem',
  }}>
    {children}
  </div>
)

export default CodeSlide;
